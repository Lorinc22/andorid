<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/RelativeLayout"
    android:gravity="center"

    tools:context=".MainActivity">


    <EditText
        android:id="@+id/textInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Írj be valami szöveget"
       />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NAGY BETÜS SZÖVEG"
        android:id="@+id/btnToUppercase"
        android:layout_below="@+id/textInput"
         />

    <Button
        android:id="@+id/btnToLowercase"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnToUppercase"
        android:text="KIS BETÜS SZÖVEG" />

    <Button
        android:id="@+id/btnColor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="VÉLETLEN SZÍN GENERÁLÁS"
        android:layout_below="@id/btnToLowercase" />
    <TextView
        android:id="@+id/textOutput"
        android:layout_width="189dp"
        android:layout_height="42dp"
        android:text="Valami Szöveg Bármi !"
        android:textAlignment="center"
        android:layout_below="@id/btnColor"

        />


</RelativeLayout>